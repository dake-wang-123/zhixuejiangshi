.grow { flex: 1; min-width: 0; }
.mini { margin-right: 12rpx; }
.wrap { flex-wrap: wrap; }
.chips { flex-wrap: wrap; }
.area { min-height: 260rpx; }
.outline {
  max-height: 220rpx;
  overflow: hidden;
}
